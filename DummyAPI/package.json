{
  "name": "dummyapi",
  "version": "1.0.0",
  "description": "Fake REST API for testing",
  "main": "index.js",
  "start" : "Port = #### react-scripts start",
  "scripts": {
    "json:server": "json-server --watch data.json --routes routes.json --port 8080"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
